server:
  host: "0.0.0.0"
  port: 8081
  mode: "debug"

discovery:
  type: "static"

instances:
  - name: "test-mysql"
    type: "mysql"
    host: "localhost"
    port: 3306
    username: "root"
    password: "password"
    database: "test"
